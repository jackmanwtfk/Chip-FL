数据预处理流程

这个项目的输入不是原始 Verilog 直接送进模型，而是先准备成 DGL 图数据。训练时，系统首先从图文件清单和数据划分文件中读取所有电路样本，然后按 train/val/test 划分加载每个电路图。对应的数据入口在 data_hetero.py (line 168) 和联邦训练器里的 trainer.py (line 1310)。

加载图之后，首先做的是基础清洗和数值预处理。这里会根据任务配置删除不需要预测的标签，比如 slew、netdelay 或 celldelay，然后对节点特征中的电容部分做缩放，对 pin location 做 min-max 归一化(对电容特征做缩放，是为了让量级较小但物理上重要的特征在网络中更容易被利用；对 pin location 做 min-max 归一化，是为了消除不同电路绝对尺寸差异的影响，让模型更多学习相对位置关系而不是绝对坐标值)。接着，为了让时序标签分布更稳定，代码会对 netdelay 做对数变换（netdelay的跨度更大，所以先做了对数变换来压缩分布、提升训练稳定性）.对应实现集中在 data_hetero.py (line 267)。


除了异构图本身，系统还会为每个样本构造一个辅助字典 ts，里面包含输入节点、输出节点、端点节点、拓扑层次以及一个由异构图转换得到的同构图 homo。这个 homo 图不是给主模型直接用的，而是专门提供给图编码器 encoder。对应代码在 data_hetero.py (line 344)。

到了联邦学习阶段，本项目不是先把整个训练集切成很多小子图再随机分配，而是先把完整电路分配到不同 client，再在每个 client 内部按拓扑层次切分子图。这样做可以避免同一个父电路的强相关子图落到不同 client，减少信息泄漏，也更符合真实联邦场景。对应逻辑在 trainer.py (line 1350) 和 trainer.py (line 1292)。

最后，模型真正前向之前，还会做一次特征增强。系统先用 encoder 在同构图 homo 上提取每个节点的潜在表示 latent，再对所有节点的 latent 求平均得到整张图的全局表示 global_embedding，最后把原始节点特征、节点潜在特征和全局表示拼接起来，再送进主模型进行 netdelay/celldelay 预测。对应代码在 client_full.py (line 733)。

